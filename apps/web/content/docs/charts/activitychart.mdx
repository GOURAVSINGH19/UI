---
title: "Activity Chart"
description: "A dual-bar activity chart with time period filtering and animations"
---

import ActivityChart from "../../../components/charts/ActivityChart"
import { DocsPreview, DocsTable } from "../../../components/docs/DocsSurface"
import { ComponentPreviewTabs } from "../../../components/ComponentPreview"
import { ComponentSource } from "../../../components/ComponentSource"

<Callout type="info" title="Visualization">
  Perfect for tracking user progress, activity logs, or comparative statistics over flexible time ranges.
</Callout>

## Preview

<ComponentPreviewTabs
  component={<ActivityChart />}
  source={
    <ComponentSource
      src="components/charts/ActivityChart.tsx"
      title="ActivityChart.tsx"
      language="tsx"
    />
  }
/>

## Usage

```tsx
import ActivityChart from '@/components/charts/ActivityChart'

const myData = [
  { day: "Sun", examPassed: 10, lessonTaken: 20 },
  { day: "Mon", examPassed: 30, lessonTaken: 15 },
  // ...
]

export default function MyPage() {
  return (
    <div className="p-4">
      <ActivityChart 
        data={myData} 
        firstName="Exams" 
        secondName="Lessons" 
      />
    </div>
  )
}
```

## API

<DocsTable
  headers={["Prop", "Type", "Description"]}
  rows={[
    [<code key="data">data</code>, "ChartData[]", "Optional: Array of data objects containing day(string), examPassed(number), and lessonTaken(number)."],
    [<code key="firstName">firstName</code>, "string", "Optional: Label for the first data series (default: 'Exam Passed')."],
    [<code key="secondName">secondName</code>, "string", "Optional: Label for the second data series (default: 'Lesson Taken')."],
  ]}
/>

## Customization

The component responds to data changes with smooth height transitions using `framer-motion`.

<Callout type="warning" title="Responsive Design">
  The chart is built to be responsive but works best within a container that provides adequate width for the columns.
</Callout>
