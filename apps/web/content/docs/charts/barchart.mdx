---
title: "Bar Chart"
description: "Beautiful and customizable charts for data visualization"
---

import { AnalyticsLineChart } from "../../../components/charts/Barchart"
import { DocsPreview, DocsTable } from "../../../components/docs/DocsSurface"
import { ComponentPreviewTabs } from "../../../components/ComponentPreview"
import { ComponentSource } from "../../../components/ComponentSource"

## Overview

The chart components provide flexible and customizable ways to display data in various formats. Our chart library uses Recharts under the hood, providing powerful data visualization capabilities with beautiful styling.

<Callout type="info" title="Data Visualization">
  Charts are ideal for visualizing trends, comparing values, and presenting data in an easy-to-understand format. Choose the right chart type based on your data and use case.
</Callout>

## When to use

- Visualizing trends over time
- Comparing values across different categories
- Displaying analytics and metrics
- Presenting statistical data and insights

## Installation

Copy the component code into your project — no package install required. If you use Recharts, ensure it's available in your project:

```bash
pnpm add recharts
```


## Preview

<ComponentPreviewTabs
  component={<AnalyticsLineChart />}
  source={
    <ComponentSource
      src="components/charts/Barchart.tsx"
      title="Barchart.tsx"
      language="tsx"
    />
  }
/>

## Usage

Here is a minimal usage example showing how to include the chart in a page or dashboard. Copy the code into a React/Next component.

```tsx
import React from 'react'
import { AnalyticsLineChart } from '@/components/charts/Barchart'

export default function Dashboard() {
  return (
    <div className="p-6">
      <h2 className="text-lg font-medium mb-4">Monthly Active Users</h2>
      <AnalyticsLineChart />
    </div>
  )
}
```

## Anatomy

- `AnalyticsLineChart` – wrapper that composes `ResponsiveContainer` and `AreaChart` with styles and tooltip
- `CustomTooltip` – lightweight tooltip renderer used by the chart

## API

<DocsTable
  headers={["Prop", "Type", "Description"]}
  rows={[
    [<code key="data">data</code>, "Array<{ name: string; value: number }>", "Optional: pass an array of data points to render a custom series."],
    [<code key="width">width</code>, "number|string", "Optional: overrides container width (default: responsive)."],
    [<code key="height">height</code>, "number|string", "Optional: overrides container height (default: responsive)."],
  ]}
/> 

## Customization

Chart components can be customized with different colors, sizes, and data configurations. The components use Recharts, which provides extensive customization options.

<Callout type="warning" title="Data Format">
  Ensure your data is properly formatted before passing it to the chart component. Data should be an array of objects with consistent keys.
</Callout>

## Best Practices

- Keep the number of data points reasonable for readability
- Use consistent colors that match your theme
- Provide clear labels, legends, and tooltips
- Ensure sufficient contrast for accessibility
- Use responsive containers for mobile compatibility

<Callout type="success" title="Pro Tip">
  For better performance with large datasets, consider implementing data aggregation, pagination, or virtualization for your charts.
</Callout>
