---
title: Slider Score
description: A slider component with score visualization and number flow animation.
---

import { SliderScore } from "../../../components/docs/SliderScore"
import { DocsPreview, DocsTable } from "../../../components/docs/DocsSurface"


## Installation

```bash
pnpm add @number-flow/react
```

## Preview

<ComponentPreviewTabs
  component={<SliderScore />}
  source={
    <ComponentSource
      src="components/docs/SliderScore.tsx"
      title="SliderScore.tsx"
      language="tsx"
    />
  }
/>

## API Surface

<DocsTable
  headers={["Prop", "Type", "Default", "Notes"]}
  rows={[
    [
      <code key="initial">initial</code>,
      <code key="initial-type">number</code>,
      <code key="initial-default">0</code>,
      "The initial value of the score slider.",
    ],
    [
      <code key="className">className</code>,
      <code key="className-type">string</code>,
      <code key="className-default">-</code>,
      "Additional styles for the component.",
    ],
  ]}
/>

