---
title: Bounce Text Reveal
description: A scroll-triggered text reveal animation with bouncing characters.
---

import { BounceTextReveal } from "../../../components/docs/BounceTextReveal"
import { DocsPreview, DocsTable } from "../../../components/docs/DocsSurface"
import { ComponentPreviewTabs } from "../../../components/ComponentPreview"
import { ComponentSource } from "../../../components/ComponentSource"

## Installation

```bash
pnpm add gsap @gsap/react   
```

## Preview
<ComponentPreviewTabs
  component={<BounceTextReveal text="Your Text Here" />}
  source={
    <ComponentSource
      src="components/docs/BounceTextReveal.tsx"
      title="BounceTextReveal.tsx"
      language="tsx"
    />
  }
/>

## Usage

```tsx
import { BounceTextReveal } from "@/components/docs/BounceTextReveal"

export default function BounceTextRevealDemo() {
  return (
    <div className="overflow-hidden">
      <BounceTextReveal text="Your Text Here" />
    </div>
  )
}
```

## API Surface

<DocsTable
  headers={["Prop", "Type", "Default", "Notes"]}
  rows={[
    [
      <code key="text">text</code>,
      <code key="text-type">string</code>,
      <code key="text-default">required</code>,
      "The text to be revealed.",
    ],
    [
      <code key="className">className</code>,
      <code key="className-type">string</code>,
      <code key="className-default">-</code>,
      "Additional styles for the container.",
    ],
  ]}
/>
